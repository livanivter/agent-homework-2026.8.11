# 学术论文阅读助手 Paper Reader —— 实现计划

> 依据《Agent 专题》课后作业(季玮晔,2026.07.29)设计
> 交付形态:双形态 —— **① 自包含 ReAct Agent(`agent.py`)+ ② Claude Code Skill**(本项目提供 3 论文工具 + 记忆 + 提示词层;agent 循环自写或由 harness 提供)
> 命名规范:姓名-学号-Agent作业 | 提交:markdown/PDF 报告 + GitHub/Gitee 源码

---

## 0. 作业要求(原文)

1. 设计一个垂直领域 Agent 产品,至少具备**三个工具**,并具备**记忆能力**
2. 针对自己的 Agent 找出三个任务(**成功/边界/失败**),分析他们的实现过程
3. 刁难成熟 Agent 产品(不限,如 Claude Code 等),出一个他们解决会失败的问题
4. 形成实验报告,使用 **Vibe-Coding** 完成,给出**所有提示词**,以及**调试过程及心得**
5. 报告以 markdown/PDF 两种形式提交;源码提交至 Github/Gitee;邮件命名 `姓名-学号-Agent作业`

---

## 1. 产品定位

- **名称:** Paper Reader —— 学术论文阅读助手
- **垂直领域:** 学术论文阅读与分析(面向研究生科研场景)
- **核心分析链:** `写了什么(事实) → 和别人比有什么不同(关系) → 为什么要这么写(意图)`
- **Agent 架构归属:** Agent = LLM + Tools + Memory,两条运行路径
  - 路径A(自包含 ReAct Agent,`scripts/agent.py`):自己的循环 + DeepSeek function calling,不依赖第三方框架
  - 路径B(Claude Code Skill):harness 提供循环,三个脚本作为工具
  - 两条路径共用:Tools = 三个论文脚本 + 5 个 agent 工具;Memory = `memory/` 下两个 JSON
- **Agent 循环:** 核心 ReAct 循环约 15 行(整脚本 228 行),呼应课件 Agent Loop 伪代码

### 架构决策(2026-08-11 定,随实现演进)

- 三脚本做**确定性预处理**(PDF 解析 / section 切分 / 硬统计),推理交给 LLM 层——工具不依赖特定模型、可独立测试。
- **三条运行路径**(实现后确认):
  1. 素材模式:脚本输出分节文本 + 硬统计,交给 Claude Code harness 推理;
  2. `--llm` 独立模式:脚本自己调 LLM(需 API key);
  3. `agent.py` 自包含 ReAct 循环:DeepSeek function calling + 5 工具(含 read_memory/update_profile),不依赖 Claude Code。
- **LLM 接入**:支持 anthropic / openai / deepseek(OpenAI 兼容),默认 deepseek-v4-flash;key 从环境变量或 `.env` 读取(环境变量优先);`DEEPSEEK_BASE_URL` 可覆盖网关。

---

## 2. 三个工具设计

### 工具① `parse_paper_core.py` —— 解析论文核心内容

**定位:** 把一篇 PDF 变成结构化"论文卡片",是工具②③的公共地基。

**输入/输出:**
- 输入:PDF 路径(可选 `--json` 输出原始 JSON、`--llm` 独立模式)
- 输出:七字段卡片;方法字段采用**五层抽取**

**五层方法抽取(L0~L4):**
| 层级 | 内容 | 作用 |
|---|---|---|
| L0 核心思想 | 一句话:用什么思路解决什么问题(领域无关) | 跨领域可比锚点 |
| L1 形式化 | 输入/输出/优化目标/约束 | 数学结构化 |
| L2 方法框架 | 组件划分 + 数据流(组件/职责/喂给谁) | 架构图文字化 |
| L3 技术细节 | 模型结构/训练(损失/优化器/策略)/数据处理/复杂度 | 领域相关细节 |
| L4 卖点与范式 | 作者声称创新点 1-3 条 + `abstract_level` 范式标签 | 对比锚点 |

`abstract_level` 取值:`end_to_end_learning / symbolic / search_heuristic / system_engineering / theory / hybrid`。

**完整卡片字段:**
```
title / abstract / problem / motivation
method:{ core_idea, formulation{input,output,objective,constraints},
         framework[{component,role,flow}],
         details{model_structure, training{loss,optimizer,strategy}, data{source,preprocess,augmentation}, complexity},
         highlights[], abstract_level }
experiments:{ datasets[], baselines[], metrics[], key_results }
conclusion / limitations
```

**流程:** 文件校验(存在/大小≤50MB/扩展名)→ 文本提取(多级回退)→ section 定位 → 卡片生成(harness LLM 或 `--llm`)→ 缓存 JSON(sha1 文件名)→ 追加阅读记录。

**失败处理(重要):** 扫描版/加密 PDF → 明确报错并说明原因,绝不硬编。

### 工具② `compare_papers.py` —— N 篇分层对比

**定位:** 对比 **2~5 篇**论文;同领域走严格逐字段对比,跨领域(大偏差)升到范式层结构对比。

**输入:** 2~5 个 PDF 路径(顺序即编号)。

**可比性判定(工具灵魂):**
| 判定 | 含义 | 对比方式 |
|---|---|---|
| 强可比 | 同问题+同指标 | 逐字段矩阵,可谈结果高低 |
| 部分可比 | 同问题+不同方法/指标 | 对齐到"问题-方法"维度,指标差异标注 |
| 跨领域 | 问题不同 | 范式映射表 + 成熟度 + 迁移启示 |

判定依据:LLM + 硬信号(标题/关键词重叠、引用重叠、abstract_level 分布)。

**输出报告结构:**
```
▶ 可比性判定表(每两两一组)
▶ [同领域] 逐字段对比矩阵 + 差异/共同/⚠冲突检测
▶ [跨领域] 范式映射表(问题类型/范式/证据/贡献/成熟度)
▶ 关系图谱(竞争/启发/互补/无关,两两判定)
▶ 横向迁移启示(跨领域方法论借鉴点)
▶ ⚠ 冲突与不可比声明(承认不可比 = 成功输出)
```

### 工具③ `analyze_narrative.py` —— 论文叙事剖析

**定位:** 逆向工程作者的写作策略;"AI 分析 AI 写作"。

**输入:** PDF 路径 + 可选 `--aspect`(只剖析某维度)。

**输出:8 维度叙事地图 + 硬统计 + overclaim 检测:**
```
1. hook_strategy    开篇钩子:痛点焦虑/研究空白/数据指标/趋势大势
2. gap_framing      空白怎么被文献"围"出来
3. positioning      跟前人区别:first宣称/强主张/增量/跨界迁移
4. evidence_arc     实验编排逻辑:先弱后强/消融/对比压倒
5. rhetoric         话术特征:novel/state-of-the-art 密度、we believe 等模糊词
6. limitation       局限写法:主动交代/轻描淡写/回避
7. target_reader    写给谁看
8. overall_plot     一句话故事线
```

**硬统计(LLM 之外客观证据):** 各 section 篇幅占比、引文密度(Intro vs Method)、声称词/模糊词词频、"we/our" 密度。

**overclaim 检测(报告加分项):** 比对"摘要声称"vs"正文证据"是否匹配,检测过度声称。

---

## 3. 记忆设计(作业硬性要求)

| 文件 | 内容 | 维护方 | 读写时机 |
|---|---|---|---|
| `memory/user_profile.json` | 用户偏好:领域方向、常看会议、摘要深度偏好、对比上限 | Claude(Skill 路径)/ agent.py 的 update_profile 工具 | 任务前读、任务后更新 |
| `memory/reading_history.json` | 每篇处理记录:文件/title/状态/时间/工具 | 脚本自动追加 | 每次成功/失败处理 |

记忆演示闭环:任务前读 `user_profile` 调整输出(如"用户只要方法部分")→ 任务后脚本记 `reading_history` → 下次可参考"读史"。

---

## 4. Skill 封装结构

```
C:\Users\Double\Desktop\work\paper-reader-skill\
├── SKILL.md                     # 触发条件/工具调用方式/工作流/记忆维护
├── scripts/
│   ├── agent.py                 # ★ 自包含 ReAct Agent 循环(DeepSeek function calling)
│   ├── parse_paper_core.py      # 工具①
│   ├── compare_papers.py        # 工具②
│   ├── analyze_narrative.py     # 工具③
│   └── lib/
│       ├── __init__.py
│       ├── paper_utils.py       # 共用底层:提取/section/统计/缓存/日志
│       └── llm.py               # 可选 LLM 客户端 + function calling(anthropic/openai/deepseek,urllib 零依赖)
├── references/
│   ├── field_schemas.md         # 字段定义与输出规范(单源真相)
│   └── prompts.md               # 提示词模板(--llm / harness 共用)
├── memory/
│   ├── user_profile.json        # 用户偏好(agent/Skill 维护)
│   └── reading_history.json     # 阅读记录(脚本自动追加,gitignore)
├── reports/
│   └── 调试过程报告.md           # 作业报告初稿
├── cache/                       # 卡片缓存(gitignore)
├── .env                         # API key(本机,gitignore)
└── .env.example                 # key 配置模板(提交)
```

---

## 5. 测试用例设计(成功/边界/失败 = 第②题素材)

| 工具 | 成功 | 边界 | 失败 |
|---|---|---|---|
| ① parse | 正常英文论文 → 完整卡片 | 超长论文截断;理论论文方法缺 L3;扫描版降级报错 | 加密 PDF;非论文 PDF;路径不存在 |
| ② compare | 同领域 2 篇 → 矩阵+冲突 | 跨领域 3~4 篇 → 范式映射;N=5 上限;部分可比 | 输入非 PDF;两篇风马牛不相及 → 不可比声明 |
| ③ narrative | 经典论文 → 8 维度地图 | `--aspect` 单维度;超短论文 | 纯图表论文无正文;overclaim 检测失败声明 |

---

## 6. 交付物

1. 源码(本目录,git init 后推 GitHub/Gitee)
2. 报告(markdown + PDF):产品说明、三工具设计、提示词清单、调试过程、三任务分析、刁难记录
3. 三张演示用测试 PDF(同领域对 + 跨领域组)

---

## 7. Vibe-Coding 提示词清单(初版,报告需附全部)

1. **初始化:** "创建 Claude Code Skill 骨架 paper-reader-skill,含 SKILL.md/scripts/lib/references/memory,git init"
2. **工具①:** "实现 parse_paper_core.py:用 pdftotext 提取文本,section 定位,输出七字段卡片 JSON;代码放 scripts/,共用逻辑进 lib/paper_utils.py"
3. **工具②:** "实现 compare_papers.py:2~5 篇,可比性判定,同领域矩阵/跨领域范式映射,输出 markdown 报告"
4. **工具③:** "实现 analyze_narrative.py:硬统计 + 8 维度叙事地图 + overclaim 检测"
5. **记忆:** "脚本成功后把 title/status/时间追加进 memory/reading_history.json"
6. **封装:** "写 SKILL.md:触发条件、工具调用命令、工作流(读记忆→选工具→跑脚本→更新记忆)"
7. **测试:** "用我给的 PDF 测试三个工具,记录成功/边界/失败输出"
8. **报告:** "把调试过程整理成 markdown 报告,含所有提示词"
9. `修 Windows UTF-8 输出:stdout 重定向默认 GBK,强制 UTF-8`
10. `修复两栏 PDF section 定位失败:raw 模式优先 + 按栏块切分判标题`

> 完整提示词清单(10 条)见 `reports/调试过程报告.md` 第五节。

---

## 8. 实现步骤(顺序)

1. [x] 本计划文档
2. [x] 项目骨架 + git init
3. [x] `lib/paper_utils.py`(提取/定位/统计/缓存/日志)
4. [x] `lib/llm.py`(可选)
5. [x] 工具① parse_paper_core.py
6. [x] 工具② compare_papers.py
7. [x] 工具③ analyze_narrative.py
8. [x] 记忆两文件 + 默认内容
9. [x] SKILL.md + references(字段定义/提示词模板)
10. [x] 端到端测试(非 LLM 部分必测;有 key 测 --llm)
11. [x] git 首次提交
12. [x] agent.py 自包含 ReAct 循环(5 工具 + 记忆工具 read/write),实测记忆闭环
13. [x] 报告初稿(reports/调试过程报告.md)
14. [x] 报告更新:阶段5(记忆不记文件路径)+ 第③题刁难实验(实测清单)
